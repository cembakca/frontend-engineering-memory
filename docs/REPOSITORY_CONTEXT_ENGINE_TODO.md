# Repository Context Engine — TODO

**Başlangıç:** 31 Ağustos 2026  
**Pilot:** `hangikredi.aboutus.fe.next`  
**North star:** Belirli bir Git snapshot'ındaki repository gerçeklerinden, verilen mühendislik görevi için minimum, güncel ve kanıtlanabilir context'i derlemek.

Bu dosya projenin kalıcı iş sırasıdır. Yeni fikirler doğrudan implementasyona alınmaz; önce buradaki ölçüm ve bağımlılık sırasına yerleştirilir.

Durumlar: `[ ]` bekliyor, `[-]` devam ediyor, `[x]` tamamlandı.

## A. Ölçüm ve ürün sözleşmesi

- [x] **RCE-001 — Evaluation contract ve golden-question kataloğu.** Pilot için görev sınıfları, 50 gerçek soru, evidence beklentileri, A/B protokolü ve başarı eşikleri tanımlandı. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_EVALUATION.md`.
- [x] **RCE-002 — Memory açık/kapalı A/B baseline.** İlk 15 vaka `config/context-engine-eval.json` ile makine-okunur hale getirildi; `pnpm memory context-eval` runner'ı yazıldı ve çalıştırıldı. Sonuç: median context tasarrufu %84,4 (hedef ≥%40 ✅), mean evidence recall 0,54 (hedef ≥0,90 ❌), strict doğruluk 1/7 (hedef %100 ❌). Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_BASELINE.md`, ham koşu `eval-results/rce-002-run-1.json`. **Karar (31 Ağu 2026):** deterministik sonuçlar yeterince açık olduğu için canlı agent A/B'si (faz 2) atlandı; run-book gerekirse diye `docs/RCE-002-AB-RUNBOOK.md` içinde duruyor.
- [x] **RCE-003 — Retrieval miss taksonomisi.** Sekiz sınıf ölçülebilir sinyal, sahip katman ve düzeltecek maddeyle tanımlandı; sınıflandırma harness'a deterministik kural olarak gömüldü (regresyon verisi olarak tekrar üretilebilir). Sonuç: 4 `wrong-intent`, 4 `missing-fact`, 2 `agent-policy`, 1 `unsupported-inference`, 4 temiz. Yapısal kök neden ölçüldü: evidence modeli iki uçlu (route memory'leri ortalama 20,3 dosya gösteriyor, diğer tüm tipler tam 1) ve 63 source dosyasının yalnız %43'ünün focused fact'i var. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_MISS_TAXONOMY.md`.
- [x] **RCE-004 — Context ekonomi sözleşmesi.** Beş kalemli defter tanımlandı; ölçülebilir üçü `pnpm memory context-economy` ile ölçülüyor, client'a ait ikisi `null` bırakılıp elle dolduruluyor. Ölçüm: sabit oturum maliyeti 1.998 token (eager client) / 512 token (deferred şema), bunun %21'i gereksiz yüzey. Nominal medyan tasarruf +3.645 token/soru, **etkin medyan −883** — motor 15 sorunun 11'inde source okumasının yerine geçemediği için net kaybettiriyor. Merkezi kural: context ekonomisi doğruluğun türevidir; etkin tasarruf pozitife dönmeden nominal tasarruf başarı sayılmaz. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_ECONOMY.md`, ham koşu `eval-results/rce-004-economy-1.json`.

## B. Bilgi kalitesi ve kanonik model

- [x] **RCE-005 — Entity/evidence normalizasyon tasarımı.** Üç katmanlı kanonik model tanımlandı: `entities` (ne) / `entity_occurrences` (nerede, `role` ile) / `memory_entities` (iddia bağı). Tek kural: occurrence asla identity'nin parçası olamaz — bugünkü `UNIQUE(...,source_file)` ve occurrence başına yeni memory satırı bu kuralı ihlal ediyor. Ölçülen baseline `quality` raporuna kalıcı metrik olarak eklendi: `duplicationRatio` 0,3886, `environmentOnlyConfigMemories` 66, `broadEvidenceMemories` 9, `canonicalSubjects` 107/175. Doğrulanabilir hedefler ve migration adımları belgelendi. **Uygulama RCE-010 ile aynı migration'da yapılmalı** — yalnız normalizasyon memory sayısını düşürür ama `missing-fact` sınıfını çözmez. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_NORMALIZATION.md`.
- [x] **RCE-006 — Değer üretmeyen memory politikası.** Beş kabul kuralı (R1 path-derivable, R2 model-redundant, R3 saturated-signal, R4 occurrence-not-fact, R5 quotable-evidence) ve tip başına verdict tablosu tanımlandı; makine-okunur hali `config/memory-acceptance.json`. Ölçüm: 175 memory → 94 benzersiz şablon; 54 kayıt reddedilir (`business_capability` 9'unun tamamı dosya yolu dışında birebir aynı, `dependency` 31 kayıt `dependencies` tablosunun 1:1 kopyası, `performance_observation` 10 kaydı `source-facts.ts:109`'daki `/^(next\/dynamic|react)$/` regex'inin false positive'i), 66 kayıt normalize edilir, 55 korunur → **175'ten 74 kanonik fact'e (%58 azalma)**. **Düzeltme:** TODO'nun varsaydığı generated/inactive gürültüsü ölçümde doğrulanmadı — aktif memory'lerde 0 generated evidence, FTS sızıntısı yok; kalan tek konu 859 pasif kaydın retention'ı (RCE-027). Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_ACCEPTANCE.md`.
- [x] **RCE-007 — Fact confidence sözleşmesi.** Dört seviye, zorunlu evidence koşulları ve seviye başına cevap dili tanımlandı; beşinci durum (kayıt yok → çekinme) eklendi. Ölçüm: `distinctConfidenceLevels` = **1** — 175 kaydın tamamı `verified`/`deterministic`, `quality_score` 0 kayıtta dolu, ve bu tek değer pack'e hiç ulaşmıyor. Kanıt olarak `/` route'unun `evidence_json`'u kullanıldı: birinci satır kanıt değil **kanıt yokluğu** ("without detected dynamic trigger"), diğer ikisi **başka dosyalardan** (`menu.ts`, `gateway.ts`) geliyor, kayıt yine de `verified`. İki yeni kural: yokluktan türetme `basis: "default"` taşımak zorunda, ve `observed` kendi dosyası dışında evidence taşıyamaz. Temizlik sonrası kalan 74 fact'in 64'ü `observed`, 10'u `derived`. **Sıra kuralı:** `confidence` alanı seviyeler gerçekten atanana kadar pack'e eklenmemeli — şimdi eklemek sahte güvence üretir. Baseline `quality` raporuna eklendi. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_CONFIDENCE.md`.
- [x] **RCE-008 — Analyzer semantic correctness paketi.** 8 characterization vakası + open-gap defteri yazıldı (`test/semantic-correctness.test.ts`); `expected` ile `current` yan yana tutuluyor, assertion `current`'a karşı yapılıyor, böylece boşluklar açıkken suite yeşil kalıyor ama davranış değiştiği an kırmızıya dönüyor. Sinyal mekanizması `RCE-008-B` üzerinde simüle edilerek doğrulandı. Altı kusur bulundu: `next/navigation` kontrol akışı hiç modellenmiyor, erişilebilir modülün sinyali sayfanın açık direktifini eziyor, sayfa sınıflandırıcısı kütüphane modüllerine uygulanıyor, segment direktifi kayıtta saklanmıyor, `rewrites()` delegation'ı modellenmiyor, yokluktan varsayılan kesin gibi kaydediliyor. **Düzeltme:** ilk değerlendirmedeki "5 `force-static` route'u yanlış `isr`" tespiti Next.js 16 dokümanıyla (Context7) çürütüldü — `force-static` sayfaları `revalidate` ile yenilenebilir, etiket savunulabilir; asıl kusur direktifin kayıttan düşmesi. Pilot: 9 kayıttan 1 yanlış, 1 eksik, 5 kayıplı, 2 doğru + 2 delege namespace envanter dışı. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_SEMANTIC_CORRECTNESS.md`.

## C. Repository semantic graph

- [x] **RCE-009 — Graph ontology.** 9 node kind ve 11 saklanan kenar tanımlandı; makine-okunur hali `config/graph-ontology.json`. Ontolojiyi belirleyen ölçüm: `ContactForm.tsx`'in import closure'ı 14 dosya ama gerçekte kullandığı dış sembol ~6 ve bunlar 2 modülde — fark barrel re-export'tan geliyor. Merkez kural: **erişilebilirlik katkı değildir**; `imports` kenarı zayıf işaretlenir ve tek başına davranışsal iddia gerekçelendiremez. Üç tasarım kararı: (1) `affected-by` **saklanmaz**, ters closure ile hesaplanır — saklanan ters kenar girdisi değişince bayatlar; (2) `component` ile `symbol` aynı key uzayını paylaşır, sınıflandırma ikinci kimlik yaratamaz; (3) `capability` yalnız human-approved kaynaktan üretilir, yoldan çıkarılmaz. Her kenar kanıt konumu ve confidence taşımak zorunda. RCE-002'de başarısız 8 vakanın her biri için gereken kenar zinciri tabloya döküldü. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_GRAPH_ONTOLOGY.md`.
- [x] **RCE-010 — Symbol/component graph extraction.** `src/analyzers/symbol-graph.ts` yazıldı: TypeScript AST'sinden `exports`, `calls`, `renders`, `references`, `reads`, `fetches`, `submits-to` kenarları çıkarıyor. Pilot: **63 dosyadan 484 tipli, yönlü, kanıt konumlu kenar**. Üç yapısal problem çözüldü: (1) sabit çözümü — `INSERT_COMMENT_URL` çözülüp `submits-to` kenarı kuruluyor (`derived`/`constant-resolution`), flow vakalarını kıran eksik buydu; (2) barrel re-export takibi — kenar `@/lib/captcha` yerine `get-captcha-token.ts`'e gidiyor; (3) endpoint şablonunda config anahtarı korunuyor (`POST ${GATEWAY_URL}/...`). **Uygulama ontolojiye geri besledi:** I05 ilk denemede çözülmedi çünkü `hcaptchaAdapter` record literalinde referans veriliyor, çağrılmıyor — `references` kenarı eklendi (RCE-009 güncellendi), modül seviyesi binding node sayıldı ve aynı-modül referansları kaydedildi. Doğrulama: I01 8 dosya, I05 5 dosya, I02 dört `fetches` + `reads`, F01 zinciri baştan sona kuruluyor. **Sapma:** I05 iki API handler'ını döndürmüyor ve bu doğru — handler'lar adapter'a değil `captcha-headers.ts`'e bağlı; RCE-002 vaka beklentisi düzeltilmeli. 8 test eklendi, tam suite 37 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_SYMBOL_GRAPH.md`.
- [x] **RCE-011 — Flow traversal.** `src/retrieval/flow.ts` yazıldı: kenar kümesini sıralı, kanıtlı akışa çeviriyor. İki eksen: kardeş adımlar `startLine`'a göre sıralanır (düz akışlı async kodda yürütme sırası yaklaşımı), sonra **anlamlı yaprağa ulaşmayan dallar budanır**. Budama olmadan saf DFS 22 adımın 15'ini tek yardımcının iç detayına harcıyordu; `parseContactForm → readText` düşer, `getCaptchaAuth → executeCaptcha` altında captcha zinciri olduğu için kalır. Pilot F01: **19 adım, 59 budanmış**, istemciden upstream'e kesintisiz — `boundary: "client-to-server"` alanı tarayıcıdan sunucuya geçişi işaretliyor. RCE-002'de bu vaka "parçaları buldu, akışı bağlayamadı" idi. Dört akış doğrulandı: F01, F02 (aynı captcha kolu, yalnız endpoint'te ayrılıyor → F04 karşılaştırması okunabilir), F06 (layout→menu→SiteChrome), F07 (slug→agreement→HTML). Çıktı `endpoints`, `config`, `prunedSteps` ve `truncated` taşıyor; her adımda `confidence`. 5 test eklendi. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_FLOW_TRAVERSAL.md`.
- [x] **RCE-012 — Change-impact traversal.** `src/retrieval/impact.ts` ile file/symbol/config seed'lerinden semantic kenarlar üzerinde ters closure hesaplanıyor; `affected-by` saklanmıyor. Çıktı kanıtlı adımları route, API route, component, config, endpoint ve test yüzeylerine ayırıyor; derinlik/adım bütçesi ve `truncated` taşıyor. Pilot: I01 manifest değişimi 11 adımda `/[slug]` + agreements UI zincirini; I02 `GATEWAY_URL` 18 adımda iki API handler, dört upstream fetch ve 8 route'u; I05 hCaptcha adapter 7 adımda registry→token→iki form zincirini buldu. I05'in iki handler beklentisi düzeltildi: handler'lar adapter'a değil bağımsız `captcha-headers.ts` sözleşmesine bağlı, bu yüzden ters dependency closure'a girmemeleri doğru. 5 test eklendi, tam suite 47 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_CHANGE_IMPACT.md`.
- [x] **RCE-013 — Test/verification graph.** `src/analyzers/verification-graph.ts` package script'lerinden `test`/`typecheck`/`lint`/`build` komutlarını, test/spec dosyalarından suite+case kimliklerini ve vaka içinde gerçekten kullanılan import/route hedeflerinden `validated-by` kenarlarını çıkarıyor. Kullanılmayan file-level import coverage sayılmıyor. `missing-test-command`, `missing-test-files` ve hedef-bazlı `unvalidated-target` boşlukları sonuç modelinin birinci sınıf parçası. Pilot D07 ölçümü: 0 test dosyası, 0 test script'i, 0 `validated-by`; 5 build varyantı + 1 lint komutu; 9/9 route doğrulanmamış. `.env.test`, `Dockerfile.test` ve `build:test` test altyapısı sayılmıyor. 6 test eklendi, tam suite 53 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_TEST_VERIFICATION.md`.

## D. Task-aware retrieval ve context compiler

- [x] **RCE-014 — Görev niyeti modeli.** `src/retrieval/intent.ts` Türkçe/İngilizce karma sorguları `lookup`, `explain-flow`, `impact`, `debug`, `implementation-plan`, `change-review`, `verify`, `unknown` sınıflarına deterministik ve açıklanabilir sinyallerle ayırıyor. Çıktı primary intent yanında confidence, intent skorları, eşleşen rule/match/weight ve alternatifleri taşıyor; eşit güçlü veya kanıtsız sorguda `unknown` ile çekiniyor. Model `understandQuery`'ye bağlandı. 50 golden soruda **50/50 beklenen sınıf**: 13 lookup, 10 flow, 8 impact, 6 implementation, 6 debug, 3 change-review, 2 verify, 2 bilinçli unknown; 20 high, 26 medium, 4 low confidence. Ek düzeltme: source path ve `https://` slash'ları artık HTTP route sanılmıyor. 6 test eklendi, tam suite 59 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_TASK_INTENT.md`.
- [x] **RCE-015 — Query plan.** `src/retrieval/query-plan.ts` RCE-014 intent'ini exact SQL, flow/impact graph, verification graph, change audit, FTS, vector ve targeted source kanallarının iki turlu planına çeviriyor. İkinci tur yalnız `empty`, `missing-evidence`, `missing-relations`, `unresolved-anchor`, `truncated` veya low intent confidence sinyaliyle açılıyor; yeterli ilk turda çalışmıyor. Plan anchor'ları route/file/symbol/config olarak çıkarıyor, kanal başına limit/karakter bütçesi ve stop/abstain koşulları taşıyor. RCE-003'te 6/15 vakayı etkileyen otomatik memory-type hard filter kaldırıldı: inferred tipler hybrid FTS/vector ranking'de yalnız +0,2 boost, sadece kullanıcı-supplied tipler filter. İlk 15 plan: 10 exact-SQL-first, 3 FTS-first, 1 verification-first, 1 change-audit-first; flow/impact vakalarının tamamı doğru traversal'a gidiyor. 6 test eklendi, tam suite 65 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_QUERY_PLAN.md`.
- [x] **RCE-016 — Dedupe ve ranking sözleşmesi.** `src/retrieval/ranking.ts` sonuçları occurrence/channel hit yerine kanonik entity üzerinde birleştiriyor; `config:GATEWAY_URL` dependency kayıtları ile configuration kayıtları aynı identity'ye çözülüyor. Skor kanal puanlarını toplamaz: task fit %25, relation coverage %25, freshness %20, evidence quality %15, entity specificity %10, yalnız en iyi kanal sırası %5. Exact query anchor task fit'i yükseltir; stale SHA ve konumsuz/inferred evidence düşer; `.env` occurrence behavior evidence sayılmaz. Hybrid search bu ranker'a geçirildi, inferred-type boost korundu, impact/debug sorularındaki “route” kelimesinin tüm route SQL envanterini açması engellendi ve dedupe öncesi FTS havuzu min 50 yapıldı. Pilot `GATEWAY_URL`: 6 config + 2 dependency kaydı → **1 kanonik entity**, 7 duplicate ID ve 6 source birleştirildi; temsilci `.env.production` yerine `next.config.ts`, I02 top-5'i exact config + iki handler route + iki fetch oldu. 6 test eklendi, tam suite 71 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_RANKING.md`.
- [x] **RCE-017 — Context-pack şemaları.** `src/retrieval/context-pack.ts` beş discriminated schema derliyor: `flow` sıralı relation/boundary; `impact` affected yüzey + ters relations; `implementation` exemplar + edit surface + verification; `debug` fact + failure path + checks; `change-review` changes + affected + verification. Her factual öğe evidence konumu/confidence taşır. Kör string truncation yok: 1.000–24.000 karakter bütçesinde düşük öncelikli array item'ları bölüm bazında düşürülür; `truncated`, `omitted`, gerçek `usedChars` ve `chars/3.5` estimated token raporlanır. Pilot 8k bütçe: F01 5.786 char/~1.654 token, I02 6.060/~1.732, P03 5.820/~1.663, D01 7.016/~2.005; dördü de truncation olmadan sığdı. 6 test eklendi. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_CONTEXT_PACKS.md`.
- [x] **RCE-018 — Answer contract.** `src/retrieval/answer-contract.ts` hem typed pack hem generic lookup için makine-okunur `answerContract` üretiyor. Claim'ler `fact`, `derived-relation`, `inference` selector'larıyla ayrılıyor; fact/inference'ın karışabildiği listelerde item-level `claimKind` var. `uncertainty.level` `none|partial|insufficient`, neden listesi, structured `missingEvidence` ve reason/priority taşıyan `sourceFallback` açık alanlar. Verification gap, boş sonuç, eksik evidence, inferred claim ve budget/traversal truncation otomatik olarak belirsizliği yükseltiyor. Nihai lookup metadata'sı da karakter bütçesine dahil; 1.200-char MCP ve uzun fallback/gap regresyonları sınırı doğruluyor. Güncel 8k pilot: F01 7.805/none, I02 7.431/none, P03 7.987/partial (18 contract ayrıntısı omitted), D01 4.100/none. Context-pack testleri 9/9, tam suite 80/80 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_ANSWER_CONTRACT.md`.
- [x] **RCE-019 — MCP yüzeyini sadeleştir.** Agent-facing MCP 9 tool'dan 3 stabil tool'a indirildi: `memory_repository` liste/get, `memory_route` liste/get, `memory_context` ise intent-aware lookup/dependency/flow/impact/debug/implementation/verification/change-review pack'i derliyor. `memory_search`/`memory_explain` ikiliği ve operasyonel `memory_quality` yüzeyden kalktı; quality/index/reconcile CLI'da kaldı. Tool schema maliyeti 5.378 char/~1.537 tokendan **2.107 char/~602 tokena (%60,8 azalma)** düştü; deferred isim maliyeti ~51'den ~14 tokena indi. Eski RCE-002 harness tool planları tarihsel baseline tekrarlanabilirliği için korundu. Ek MCP resource/prompt eklenmedi: ölçülmüş ihtiyaç yok. MCP testi hem bounded lookup hem gerçek source graph'tan flow pack'i doğruluyor; MCP + context-pack 7/7 ve tam suite 77/77 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_MCP_SURFACE.md`.

## E. Temporal ve karar hafızası

- [x] **RCE-020 — Behavior diff.** Her başarılı index aynı transaction içinde route/dependency/active fact/symbol-graph snapshot'ı kaydediyor. `diffBehaviorSnapshots` iki indexed SHA'yı route rendering/auth/cache/data/SEO, flow edge, config reader, API caller ve fact düzeyinde karşılaştırıyor; dosya listesi davranış değişikliği sayılmıyor. Bütçeli `kind: behavior-diff` pack'i `memory_context(atSha,compareToSha)` ve CLI `behavior-diff` üzerinden erişilebilir. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_BEHAVIOR_DIFF.md`.
- [x] **RCE-021 — Point-in-time context.** Kullanıcı E bölümünü istemesiyle gereksinim doğrulandı. `repository_snapshots` yalnız başarılı index transaction'ında immutable, exact SHA görünümü saklıyor; `memory_context(atSha)` ve CLI `context-at` lexical olarak hedeflenmiş active fact + graph relation pack'i döndürüyor. Arbitrary/unindexed commit sessizce HEAD'e düşmez, açık hata verir. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_POINT_IN_TIME.md`.
- [x] **RCE-022 — Decision provenance.** `repository_decisions` source code fact'lerinden ayrı: rationale yalnız `adr|pr|issue|human` kaynağı, `sourceRef`, opsiyonel full `sourceSha`, `approvedBy`, `approvedAt` ile yazılır; onaysız kayıt reddedilir. Yeni accepted karar eskisini explicit `supersedesKey` ile superseded yapar. Açık why/rationale soruları yalnız accepted kayıtlardan `kind: decision` pack'i alır; hata-nedeni soruları debug olarak kalır. CLI ingest/list eklendi. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_DECISION_PROVENANCE.md`.
- [x] **RCE-023 — Native agent memory sınırı.** Altı memory class'ı üç tekil owner'a bağlandı: kişisel tercih/episodic state → agent-native; workflow policy → repository instructions; fact/relation/human decision → context engine. `config/memory-ownership.json` ve `src/memory/ownership.ts` overlap'i makine-okunur/validate edilebilir kılıyor; native memory yalnız pointer, AGENTS/CLAUDE yalnız retrieval policy tutar. E bölümü 4 yeni temporal/decision/ownership testi, sync snapshot regresyonu ve tam suite **84/84 pass** ile kapandı. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_MEMORY_OWNERSHIP.md`.

## F. Telemetry, işletim ve rollout

- [x] **RCE-024 — Retrieval telemetry.** `retrieval_events` tablosu + `src/telemetry/retrieval-telemetry.ts` + `pnpm memory telemetry`. Gizlilik modeli **whitelist**: kaydedici kapalı/tipli event kabul eder, yalnız şemadaki alanları yazar — blacklist ileride pack'e eklenen bir alanı sızdırırdı. Fact içeriği, evidence alıntısı, kaynak kod ve config **değerleri** asla kaydedilmez; entity adları kaydedilir (anahtar adı sır değil, değeri sırdır). Soru metni varsayılan kapalı: yerine `query_hash` + `query_shape` (words/lengthBucket/turkish/hasRoutePath/hasFilePath/hasConfigKey), `MEMORY_TELEMETRY_QUERY_TEXT=1` ile açılır. TODO'nun yedi kalemi de karşılandı; intent plan'dan ayrıca alınıyor ki `implementation-plan`/`verify` ayrımı `implementation` pack kind'ına düşmesin. Miss sınıfı yalnız motorun ayırt edebildiği vakalarda atanır, gerisi `null`. Telemetri asla cevabı bozmaz (tablo silinerek test edildi) ve girdi normalize edilir — bu gerçek bir hatayı yakaladı (`answerContract.uncertainty` dizi değil `{level,reasons}`). Retention 5000, her 100 kayıtta budama. **İlk ölçüm hemen bir boşluk gösterdi:** "Değişiklikten sonra hangi komutları çalıştırmalıyım?" `unknown` sınıflandırıldı, `verify` olmalıydı → RCE-025 backlog'u. 7 test, tam suite 91 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_TELEMETRY.md`.
- [x] **RCE-025 — Answer feedback loop.** `answer_feedback` tablosu + `src/telemetry/answer-feedback.ts` + CLI `feedback add|backlog|triage` + `POST/GET /feedback`. İlke: **sinyal sayaç değil, taslak evaluation vakası üretir** — RCE-002 suite'inin tükettiği şemada. `sufficient` pozitif kontrol olarak saklanır, backlog'a girmez. **MCP yüzeyi büyütülmedi** (RCE-004'ün sabit tool maliyeti nedeniyle): dördüncü tool yerine `memory_context` pack'ine tek `telemetryEventId` alanı eklendi. Event id olmadan, yalnız soruyla da rapor verilebiliyor — soru telemetriyle aynı şekilde hash'lenip en son eşleşen olaya bağlanıyor (ilk versiyonda bu eksikti, `retrieval` bloğu boş geliyordu). **Sahip yönlendirmesi sinyalden değil nedenden türetiliyor:** `missClass=wrong-intent` veya `intent=unknown` ise RCE-014/015, `stale` ise RCE-026, `source-needed` ise RCE-010/016, diğer `wrong` ise RCE-008/007. Bu gerçek bir vakada fark yarattı — telemetrinin bulduğu `unknown` sınıflandırma hatası analyzer'a değil intent modeline yönlendi. Taslak vakada `expectedEvidence` bilerek boş: motorun kendi başarısızlığı için beklenen kanıtı kendisinin üretmesi ölçümü geçersiz kılardı. `case-created` durumu ürettiği vaka kimliği olmadan yazılamaz. 8 test, tam suite 99 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_FEEDBACK.md`.
- [x] **RCE-026 — Freshness SLO.** `src/retrieval/freshness.ts` + `config/freshness-slo.json` + CLI `freshness`. **İki soru ayrıldı:** snapshot freshness (indeks güncel commit'i mi anlatıyor) ve tree accuracy (güncel commit diskteki dosyaları mı anlatıyor) — RCE-N03 tam olarak burada başarısız olmuştu, motor birincisini cevaplayıp ikincisi hakkında sessiz kalıyordu. Altı durum: `fresh`, `tree-dirty`, `behind`, `diverged`, `never-indexed`, `unknown`; her biri cevaba zorunlu bir cümle üretiyor. Hedefler: merge→indexed gecikme 900s (ihlal 86.400s, commit zamanından ölçülür, tetikleme anından değil), drift 0 commit (uyarı ≥1, ihlal ≥10). Üçüncü hedef sayısal değil davranışsal: **`every-pack-declares-freshness`** — `memory_context` artık her pack'e `freshness` bloğu ekliyor, `fresh` durumunda bile `guidance` cümlesi taşıyor. Pilot bugün `tree-dirty` raporluyor (SHA eşit, 1 dosya uncommitted) ve doğru uyarıyı üretiyor. Önbellek 15s TTL ile ve **SHA anahtarlı değil** — drift snapshot değişmeden oluşur, SHA anahtarlı önbellek bu kontrolün varlık nedenini gizlerdi. Ölçülemeyen durum `unknown` döner, sessizce `met` sayılmaz. **Yol boyunca iki hata çıktı ve düzeltildi:** (1) blok pack derlendikten sonra ekleniyordu, bu RCE-017 bütçe sözleşmesini bozuyordu — freshness artık `CommonInput`/`BasePack` içinde, diğer içerik gibi bütçeleniyor (bütçe tabanını 400'e indirme denemesi ikinci bir testle çürütüldü: 1000 tabanı kasıtlı ve sözleşmeli); (2) zarf bütçeye girince küçük bütçede tek fact düşüyordu ve pack "No match; report the miss" diyordu — **bu yalandı**, eşleşme vardı sığmadı; artık `budgetExceeded` ile ayrılıyor ve "raise the budget rather than reporting a miss" diyor. 8 test, tam suite 107 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_FRESHNESS.md`.
- [x] **RCE-027 — Güvenlik ve veri yönetişimi.** `config/data-governance.json` + `src/security/audit.ts` + `pnpm memory security-audit` (ihlalde çıkış kodu 1). **İlke: çalıştırılamayan kural politika değildir** — her kural bir invariant kimliğine bağlı ve denetim onu gerçekten sorguluyor. Beş invariant: I1 env değerleri hiçbir tabloda yok, I2 registry sınırdır (evidence yolu repo dışına çıkmasın, indekslenmiş repo registry'de olsun), I3 AI memory'leri producer/quality/inferred/evidence taşısın, I4 telemetride fact içeriği olmasın, I5 retention pencereleri. **Denetimin boşuna geçmediği kanıtlandı:** her invariant kasten ihlal edilerek test edildi (env değeri fact'e yazıldı, evidence yolu `/etc/passwd` yapıldı, registry dışı repo eklendi, quality'siz AI memory'si, telemetriye kopyalanan içerik, 200 gün geriye tarihli deaktive kayıt). Sekizinci test ölçülemeyen durumun `pass` değil **`skipped`** döndüğünü doğruluyor. Canlı pilot: 5/5 pass, 4 `.env*` dosyasından 28 gerçek değerin hiçbiri hiçbir tabloda yok. Bilinçli sınırlar yazıldı: I1 altı karakterden kısa değerleri atlar, `decisions.rationale` ve `feedback.note` serbest metindir ve insan onayına bırakılır. 8 test, tam suite 115 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_GOVERNANCE.md`.
- [x] **RCE-028 — İkinci pilot kabul kapısı.** `config/rollout-gates.json` + `src/rollout/pilot-gate.ts` + `pnpm memory pilot-gate` (ihlalde çıkış kodu 1). **Kapı bir toplantı değil, bir komut:** ölçülmüş `context-eval`, `context-economy`, `security-audit` koşularını ve semantic gap defterini okuyup `open`/`blocked`/`insufficient-evidence` döndürüyor. **Bugünkü sonuç `blocked`, altı engelle:** recall 0,54 (≥0,90 gerek), strict 3/7 (%100 gerek), clean 4/15 (≥0,80 gerek), etkin medyan tasarruf −883 token (>0 gerek), motorun kaybettiği vaka 3/15 (≤%10 gerek), 2 bloke eden semantic gap. Tek geçen kontrol yönetişim. RCE-008 açık-boşluk defteri test dosyasından `src/analyzers/semantic-gaps.ts`'e taşındı; suite ve kapı artık aynı listeyi okuyor. `BLOCKING_SEMANTIC_GAPS` tanımlandı — sekiz boşluğun ikisi (`/` redirect regresyonu, `force-static` ezilmesi) ikinci repo'yu bloke eder çünkü **yanlış bir fact seyahat eder**; diğer altısı eksikliktir. `unknown` asla `pass` değil: koşu yoksa karar `insufficient-evidence`'a düşer, aday çeşitliliği indekslenmeden bilinemeyeceği için `unknown` dönüp neyin farklı olması gerektiğini yazar. 7 test kapının hem bloke edebildiğini hem de eşiklere gerçekten tepki verdiğini kanıtlıyor. Tam suite 122 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_ROLLOUT_GATE.md`.
- [x] **RCE-029 — Kontrollü çoklu-repository rollout.** `config/rollout-gates.json` → `multiRepository` + `src/rollout/rollout-plan.ts` + `pnpm memory rollout-status` (ilerleyemiyorsa çıkış kodu 1). Beş dalga (pilot 1 → diversity 2 → squad 4 → division 8 → fleet 16), her biri soak süresiyle. **Merkez kural: kapı geriye de bakar** — onboard edilmiş her repository sağlık tabanının üstünde olmalı; kapı açık olsa bile geri gitmiş bir repository rollout'u `hold` eder, ve sağlıklı olan diğerleri suçlanmaz (test sabitliyor). Gerekçe RCE-004'ten: ekonomi doğruluğun türevidir, bozulan motora repo eklemek bozulmayı çoğaltır. Sağlık tabanı: freshness `fresh`/`tree-dirty`, 0 güvenlik ihlali, `duplicationRatio` ≤0,05, `evidenceCoverage` ≥0,95. İşletim maliyeti **ölçülüyor**: veritabanı boyutu diskten, p95 latency telemetriden. Bugün: `hold`, currentWave `pilot`, nextWave `diversity`; iki bağımsız neden — kapı kapalı (6 engel) **ve** pilotun `duplicationRatio` 0,3886 (RCE-005 migration'ı yapılmadı). Maliyet tarafı sağlıklı (5,5 MB, p95 3.097 ms), yani darboğaz maliyet değil doğruluk. 6 test; fixture'lar gerçek Git deposuna çevrildi çünkü `unknown` freshness taban altıdır. Tam suite 128 pass. Çıktı: `docs/REPOSITORY_CONTEXT_ENGINE_ROLLOUT.md`.

## Bütüncül uygulama geçişi (31 Ağu 2026)

Tasarımı yapılmış maddeler tek geçişte uygulandı; ölçümler pilotun temiz klonunda yeniden alındı. Sonuç: recall **0,5436 → 0,9115**, temiz vaka **4/15 → 11/15**, etkin medyan tasarruf **−883 → +755 token**, memory **175 → 85**, `duplicationRatio` **0,3886 → 0**, bloke eden semantic gap **2 → 0**, test **128 → 134**. Kapı 7 kontrolün 4'ünü geçiyor.

En büyük tek kazanç koddan değil ölçümden geldi: evaluation suite'i RCE-019'da kaldırılmış beş MCP tool'unu çağırıyordu, yani hiçbir agent'ın gidemeyeceği bir yolu ölçüyordu. Gerçek yüzeye çevrilince recall tek başına 0,5615 → 0,7705 çıktı. Aynı sınıftan iki bayat kural daha düzeltildi (`agent-policy` artık "item döndü mü" değil "yetersizliğini ilan etti mi" diye bakıyor; `engineLoses` abstain vakalarını dışlıyor).

Kalan üç engel tek kök nedene iniyor: captcha modülü ve route handler'ları için **modül sözleşmesi / hata cevabı fact tipi yok** — RCE-013'ün kapsamı. Ayrıntı: `docs/REPOSITORY_CONTEXT_ENGINE_IMPLEMENTATION_PASS.md`.

## Şimdilik yapılmayacaklar

- Sırf daha fazla memory üretmek için bütün source dosyalarını AI ile özetlemek.
- Evaluation kanıtı olmadan embedding modeli veya vector store değiştirmek.
- Full source chunk'larını model context'ine veya vector indeksine gelişigüzel kopyalamak.
- Source/ADR/PR kanıtı olmadan `why` veya business rule uydurmak.
- Tek pilotun task-level doğruluğu kabul edilmeden 16 repository rollout yapmak.
